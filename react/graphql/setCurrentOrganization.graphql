mutation CurrentOrganization($orgId: ID!, $costId: ID!) {
  setCurrentOrganization(orgId: $orgId, costId: $costId)
    @context(provider: "vtex.storefront-permissions") {
    status
    message
  }
}
