query GetSellersPaginated($page: Int, $pageSize: Int) {
  getSellersPaginated(args: { page: $page, pageSize: $pageSize }) {
    pagination {
      page
      pageSize
      total
    }
    items {
      id
      name
      email
    }
  }
}
