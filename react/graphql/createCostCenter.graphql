mutation CreateCostCenter($organizationId: ID, $input: CostCenterInput!) {
  createCostCenter(organizationId: $organizationId, input: $input)
    @context(provider: "syatt.b2b-organizations-graphql") {
    id
  }
}
